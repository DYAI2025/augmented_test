# APP_URL: Optional canonical URL for generated absolute links.
# Railway exposes the public domain in the dashboard after deployment.
APP_URL="https://your-app.up.railway.app"

# PORT: Railway injects this automatically. Set it locally only when needed.
# PORT=3000
