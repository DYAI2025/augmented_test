# Augmented_Ops · Web UI Kit

High-fidelity recreation of the VSA/Augmented_Ops navy-trust web surface. Canonical reference: `vs-agentur` live site.

## Components
- `Navigation.jsx` — fixed top nav, blur + 95% white
- `HeroAgentMesh.jsx` — 2-col hero with live Agent Mesh panel on right
- `MetricStrip.jsx` — solid navy-900 strip between light sections
- `AgentCard.jsx` — agent role card; hover lifts + border navy-600
- `WorkflowFlow.jsx` — 5-column horizontal flow with human-gate highlight
- `PrincipleCard.jsx` — principle block with eyebrow + body
- `CTABand.jsx` — navy gradient CTA band (wrapped, not full-bleed)
- `Footer.jsx` — 2fr·1fr·1fr·1fr footer with legal block

Composed in `index.html` as a scrollable landing page recreation. Not real production code — cosmetic fidelity only.
